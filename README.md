Правила формата (зафиксируйте в README):
пробелы внутри полей не допускаются;
одна котировка на одну UDP-датаграмму;
буфер приёма — разумный лимит (например 2 KiB).
Файл тикеров:
один тикер на строку;
пустые строки пропускаются;
пробелы по краям обрезаются (trim).
